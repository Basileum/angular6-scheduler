import { SlotByDayPipe } from './slot-by-day.pipe';

describe('SlotByDayPipe', () => {
  it('create an instance', () => {
    const pipe = new SlotByDayPipe();
    expect(pipe).toBeTruthy();
  });
});
